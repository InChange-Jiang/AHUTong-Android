# 模块边界的存量违规清单（baseline allowlist）
#
# 规则：只减不增。
#   - 每行格式：<ruleId> <相对路径>
#   - 只有列在这里的文件可以违反对应规则；
#   - 新增文件、或未列出的文件违反规则时，ModuleBoundaryTest 会失败；
#   - 修复一条就删掉一行，最终目标是清空本文件。
#
# 规则定义见 app/src/test/java/com/ahu/ahutong/architecture/ModuleBoundaryTest.kt
#
# 2026-09-14：清空。最后两条 R2（ui 直接调用协议 API）的修法是「经数据层网关」：
#   - 空闲教室的两个教务端点搬进 data/FreeClassroomGateway，ViewModel 不再认识协议客户端；
#   - 登录页 WebView 的浏览器标识改由 AHURepository.jwxtBrowserUserAgent 提供。
# 至此边界测试无豁免：任何新增违规都会直接失败。

